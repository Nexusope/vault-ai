# Knowledge Graph Agent Prompt

Read `docs/12-knowledge-graph.md`. Given versioned analysis and embedding fixtures, generate candidate nodes/edges with canonical direction, weight, confidence, evidence, algorithm version, and tenant ownership. Preserve manual edges, quarantine low confidence, and make the projection rebuildable. Implement bounded traversal explanations and stable layout updates. Test idempotency, duplicate/symmetric edges, deletion, tenant isolation, confidence thresholds, traversal caps, and representative precision.
